services:
  backend:
    image: josephmj0303/barista-cafe-backend:latest
    container_name: barista-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: 5000
      LOG_LEVEL: INFO
    expose:
      - "5000"

  frontend:
    image: josephmj0303/barista-cafe-frontend:latest
    container_name: barista-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend
